# basic — 基础能力

连通性测试与运行环境信息。

## 工具

### echo

回显输入文本，用于测试 server 连通性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | 要回显的文本 |

**返回**：与输入完全一致的字符串。

### system_info

返回当前运行 server 的系统与解释器信息。

**返回**：包含 platform、python 版本、解释器路径、是否虚拟环境、cwd、pid 的字典。

## 依赖

无（仅使用 Python 标准库）。
