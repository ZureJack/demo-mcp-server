# time_tools — 时间相关能力

获取当前时间。

## 工具

### get_current_time

获取当前时间，支持指定 IANA 时区。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tz` | `str \| None` | IANA 时区名（如 `"Asia/Shanghai"`、`"UTC"`），不传则使用系统本地时区 |

**返回**：ISO-8601 格式的时间字符串（精确到秒）。

## 依赖

无（仅使用 Python 标准库）。
