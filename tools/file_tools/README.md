# file_tools — 文件读写能力

读取本地文本文件。

## 工具

### read_text_file

读取一个本地文本文件的内容（UTF-8 编码）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | 文件绝对路径或相对于 server 启动目录的相对路径 |
| `max_bytes` | `int` | 最多读取字节数，默认 64KB |

**返回**：文件文本内容（非法字节会被替换）。

## 依赖

无（仅使用 Python 标准库）。
